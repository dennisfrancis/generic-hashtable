
CC=gcc

CFLAGS=-Wall -O3 -march=native

# Linking 
FLAGS=

TARGET=test_ll

OBJS=	test_ll.o 	\
	dll.o

all: $(TARGET)


$(TARGET): $(OBJS)
	$(CC) $(CFLAGS) $(OBJS) -o $@  # $@ means target


.c.o:
	$(CC) -c $(CFLAGS) $<

# queue.o: queue.c queue.h
# 	$CC $CFLAGS -c queue.c

# main.o: main.c
# 	$CC $CFLAGS -c main.c

clean:
	rm $(OBJS)
